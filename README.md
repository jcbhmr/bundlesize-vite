# Vite size CLI

⚡ See how much a package weighs when imported & bundled using Vite

## Installation

```sh
npm install -D vite-size
```

## Usage

```sh
vite-size
vite-size lodash
vite-size file:./packages/my-package
```

💡 Make sure you use `npx vite-size` if you're executing it outside of your
`package.json` scripts.
